name: data
description: A new Flutter module project.
version: 1.0.0+1

environment:
  sdk: '>=3.1.5 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  dio: ^5.3.3
  json_annotation: ^4.8.1
  hive: ^2.2.3
  hive_flutter: ^1.1.0

  domain:
    path: ../domain

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  build_runner: ^2.3.3
  json_serializable: ^6.6.0
  hive_generator: ^2.0.1

flutter:
  uses-material-design: true
  module:
    androidX: true
    androidPackage: com.cookbook.data
    iosBundleIdentifier: com.cookbook.data
